// components/consent/cookie-banner.tsx
"use client";

/**
 * Cookie consent banner — disabled (owner request 2026-08-07).
 * Preferences page at `/cookies` and consent actions remain available.
 */
export function CookieBanner(_props: { show: boolean }) {
  return null;
}
